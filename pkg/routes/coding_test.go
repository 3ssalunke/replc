package routes

import (
	"testing"
)

func TestReadAndParseKubeYaml(t *testing.T) {
	// dir, err := filepath.Abs(filepath.Dir(os.Args[0]))
	// assert.Nil(t, err)

	// Construct the path to the service.yaml file
	// yamlFilePath := filepath.Join(dir, "../orchestrator/service.yaml")
	// readAndParseKubeYaml(yamlFilePath, "test")
}
