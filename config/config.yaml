web:
  hostname: "localhost"
  port: 8000

orchestrator:
  hostname: "localhost"
  port: 8001

app:
  name: "replc"
  environment: "local"

database:
  hostname: "localhost"
  port: 5432
  user: "admin"
  password: "admin"
  database: "replc"

s3:
  accesskey: ""
  secretkey: ""
  region: "ap-south-1"

k8s:
  namespace: "default"
  kubeconfigpath: "/mnt/c/Users/Suraj S/.kube/config"
  serviceyamlpath: "../orchestrator/service.yaml"
